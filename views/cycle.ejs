<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="/socket.io/socket.io.js"></script>
    <title>Document</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        img {
            width: 100%;
            height: 100%;
            overflow:hidden;
        }
    </style>
</head>
<body>
    <!-- <div style="display: flex; flex-direction: column; justify-content: center; align-items: center;"> -->
        <!-- <h1 id="text">This is 1x Speed</h1> -->
        <img src="../assets/images/robot/ robot(130).jpg" id="images" alt="" width="100%" height="100%" style="padding: 0;margin:0">
    <!-- </div> -->

    <script>
       
       var imgName = document.getElementById("images");
       var N=130;
       document.addEventListener('keypress', (event) => {
        //   var name = event.key;
        // console.log(event.key)
        if (event.key === 'f'){
        N += 2;
        if(131 < N <= 1507){
            imgName.src = `../assets/images/robot/ robot(${N}).jpg`
            // console.log(imgName.src)
        }
        // console.log(imgName.src)
        if(N>1507){
            location.href = '/thanks'
        }
        }
    }, false);

  // Alert the key name and key code on keydown
  // alert(`Key pressed ${name} \r\n Key code value: ${code}`);
    </script>
</body>
</html>